# NomadWay Backend Environment Configuration
# Copy this file to server/.env and update the values

# ===========================================
# REQUIRED SETTINGS
# ===========================================

NODE_ENV=production
PORT=3001

# Database Connection
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://nomadway:nomadway_secure_password_2024@localhost:5432/nomadway?schema=public

# JWT Secrets - MUST CHANGE IN PRODUCTION!
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-immediately
JWT_REFRESH_SECRET=your-refresh-secret-key-change-this-immediately
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://91.228.154.82

# ===========================================
# OPTIONAL SETTINGS
# ===========================================

# OpenAI API (for AI Guide and Route Builder features)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Email Configuration (for password reset, notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Cloudinary (for image uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
